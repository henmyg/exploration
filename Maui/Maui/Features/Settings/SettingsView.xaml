<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:locator="clr-namespace:Maui.Features"
             xmlns:vm="clr-namespace:Maui.Core.Features.Settings;assembly=Maui.Core"
             x:Class="Maui.Features.Settings.SettingsView"
             x:DataType="vm:ISettingsViewModel">
    <ContentView.BindingContext>
        <locator:Locator ViewModelType="{x:Type vm:ISettingsViewModel}" />
    </ContentView.BindingContext>

    <ScrollView>
        <VerticalStackLayout Spacing="0">
            <Label
                Text="{Binding Title}"
                FontSize="24"
                FontAttributes="Bold"
                Margin="20,20,20,10" />

            <!-- Configuration Section -->
            <VerticalStackLayout Spacing="0" Margin="0,10,0,0">
                <Label
                    Text="CONFIGURATION"
                    FontSize="12"
                    TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"
                    Margin="20,10,20,5" />

                <Border StrokeThickness="0" BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}">
                    <Grid RowDefinitions="Auto" ColumnDefinitions="*,Auto" Padding="20,15">
                        <VerticalStackLayout Grid.Column="0" Spacing="4">
                            <Label Text="Configuration" FontSize="16" />
                            <Label Text="Price area and other settings"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light=#CCCCCC, Dark=#666666}" />
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToConfigurationCommand}" />
                        </Grid.GestureRecognizers>
                    </Grid>
                </Border>
            </VerticalStackLayout>

            <!-- Debug Section -->
            <VerticalStackLayout Spacing="0" Margin="0,10,0,0" IsVisible="{Binding ShowDebugSection}">
                <Label
                    Text="DEBUG"
                    FontSize="12"
                    TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"
                    Margin="20,10,20,5" />

                <Border StrokeThickness="0" BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}">
                    <Grid RowDefinitions="Auto" ColumnDefinitions="*,Auto" Padding="20,15">
                        <VerticalStackLayout Grid.Column="0" Spacing="4">
                            <Label Text="View States" FontSize="16" />
                            <Label Text="Test different UI states"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light=#CCCCCC, Dark=#666666}" />
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToViewStatesCommand}" />
                        </Grid.GestureRecognizers>
                    </Grid>
                </Border>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>
